The code in this directory contains a class that is capable of injecting user functions. 
There is example code that plots the mean response over the whole image over time.
Injecting user functions in this way is likely of limited use, TBH.